import { PlaceholderPage } from '@/components/placeholder-page'

export function ExpensesPage(): React.JSX.Element {
  return (
    <PlaceholderPage
      title="Expenses"
      description="Shop expenses land in a later slice."
    />
  )
}
