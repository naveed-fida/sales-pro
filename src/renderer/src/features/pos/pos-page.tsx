import { PlaceholderPage } from '@/components/placeholder-page'

export function PosPage(): React.JSX.Element {
  return (
    <PlaceholderPage
      title="POS"
      description="The sale screen lands in a later slice. Until then, Alt+1 through Alt+8 switches pages."
    />
  )
}
