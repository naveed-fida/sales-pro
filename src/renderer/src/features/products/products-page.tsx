import { PlaceholderPage } from '@/components/placeholder-page'

export function ProductsPage(): React.JSX.Element {
  return (
    <PlaceholderPage
      title="Products"
      description="The catalog — variants, barcodes, stock — lands in a later slice."
    />
  )
}
