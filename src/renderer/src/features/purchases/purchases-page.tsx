import { PlaceholderPage } from '@/components/placeholder-page'

export function PurchasesPage(): React.JSX.Element {
  return (
    <PlaceholderPage
      title="Purchases"
      description="Supplier purchases and receiving stock lands in a later slice."
    />
  )
}
