import { PlaceholderPage } from '@/components/placeholder-page'

export function ReturnsPage(): React.JSX.Element {
  return (
    <PlaceholderPage
      title="Returns"
      description="Looking up a bill and returning lines lands in a later slice."
    />
  )
}
