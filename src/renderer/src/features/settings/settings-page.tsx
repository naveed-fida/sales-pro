import { PlaceholderPage } from '@/components/placeholder-page'

export function SettingsPage(): React.JSX.Element {
  return (
    <PlaceholderPage
      title="Settings"
      description="Shop details, receipt footer and printer selection land in a later slice."
    />
  )
}
